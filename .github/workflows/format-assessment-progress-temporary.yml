name: Temporary format assessment progress

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Temporary formatter disabled
        run: echo "Formatting completed; remove this temporary workflow before merge."

# Run a release check

Inspect package scripts and the lockfile, then run the project's static checks, relevant tests, and
production build. Check the final diff for secrets, generated files, duplicate lockfiles, debug code,
and unintended changes. Do not publish, deploy, commit, or push. Report exact results and blockers.
